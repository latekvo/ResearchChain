apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: embedder
  name: embedder
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: embedder
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: embedder
    spec:
      containers:
        - image: embedder
          name: embedder
          volumeMounts:
            - mountPath: /app/store/vector
              name: shared-vector-store
      restartPolicy: Always
      volumes:
        - name: shared-vector-store
          persistentVolumeClaim:
            claimName: shared-vector-store
